import { HighlightCompletedVideoDirective } from './highlight-completed-video.directive';

describe('HighlightCompletedVideoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletedVideoDirective();
    expect(directive).toBeTruthy();
  });
});
